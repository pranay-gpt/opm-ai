# ──────────────────────────────────────────────────────────────────────
# OPM-AI Python dependencies
#
# NOTE: OPM Flow (the simulator binary) must be installed via system packages:
#   Ubuntu/Debian:  sudo apt install opm-simulators
#   OR snap:        sudo snap install opm
#   OR build from source: https://opm-project.org
#
# opm-python / opm-common are C++ extension modules shipped WITH the
# opm-simulators apt package, NOT available on PyPI. After apt install,
# the Python bindings are at /usr/lib/python3/dist-packages/opm/
# ──────────────────────────────────────────────────────────────────────

# Simulation result reading (pure Python, on PyPI)
ecl2df>=0.17
resdata>=0.3
resfo>=4.4

# LLM providers (OpenAI-compatible REST clients)
openai>=1.40.0       # Used for NVIDIA NIM (integrate.api.nvidia.com) + fallback
groq>=0.9.0          # Groq fast inference (console.groq.com)

# UI
streamlit>=1.36.0

# Data & plotting
pandas>=2.0
numpy>=1.26
plotly>=5.22

# Testing
pytest>=8.0
pytest-cov>=5.0
