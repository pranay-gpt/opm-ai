# Core simulation
ecl2df>=0.17
opm-python>=2024.10
resfo>=4.4

# LLM providers (OpenAI-compatible)
openai>=1.40.0       # Used for both NVIDIA NIM and as fallback client
groq>=0.9.0          # Groq fast inference

# UI
streamlit>=1.36.0

# Data & plotting
pandas>=2.0
numpy>=1.26
plotly>=5.22

# Testing
pytest>=8.0
pytest-cov>=5.0
