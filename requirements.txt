# ════════════════════════════════════════════════════════════════════════
# OPM-AI Python dependencies
#
# Python version: 3.12+ (Ubuntu 24.04 default)
#
# NOTE: OPM Flow binary must be installed via system package manager:
#   sudo apt install opm-simulators
# The opm Python bindings ship WITH that apt package at:
#   /usr/lib/python3/dist-packages/opm/
# They are NOT on PyPI — do not add opm-python here.
# ════════════════════════════════════════════════════════════════════════

# ── Simulation result reading ────────────────────────────────────────────
# resdata 5.x supports Python 3.12 and 3.13 (dropped <3.12 in 5.0.0)
# ecl2df is NOT listed — it hard-requires resdata<4 which requires Python <3.12
resdata>=5.0.0
resfo>=4.4

# ── LLM providers (OpenAI-compatible REST clients) ───────────────────────
openai>=1.40.0        # NVIDIA NIM (integrate.api.nvidia.com) + fallback
groq>=0.9.0           # Groq fast inference (console.groq.com)

# ── UI ───────────────────────────────────────────────────────────────────
streamlit>=1.36.0

# ── Data & plotting ──────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.26
plotly>=5.22

# ── Testing ──────────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
